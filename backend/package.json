{
  "name": "dashboard-backend-cloudflare",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "d1:migrate": "wrangler d1 migrations apply dashboard_db",
    "d1:migrate:local": "wrangler d1 migrations apply dashboard_db --local"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260210.0",
    "typescript": "^5.8.2",
    "wrangler": "^4.50.0"
  }
}
